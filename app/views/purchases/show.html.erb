<div class="mb-4 mt-2 clearfix text-center">
  <h1 class="d-inline-block mb-0">Show Purchase</h1>
  <%= link_to ('<i class="fa fa-edit "></i>').html_safe, edit_purchase_path(@purchase), class: "pull-right" %>
  <%= delete_link(purchase_path(@purchase), "pull-right mr-3 ml-3") %>
  <%= link_to ('<i class="fa fa-download "></i>').html_safe, purchase_path(@purchase, format: :pdf), class: "pull-right" %>
</div>
<div class = "purchase-details" >
  <div class="row mt-2 mb-2">
    <div class="col-6">
      <span>Date: <%= @purchase.date %></span>
    </div>
    <div class="col-4">
      <span class="pull-right">Purchase ID: </span><br>
      <span class="pull-right">Order Value: </span>
    </div>
    <div class="col-2">
      <span class="pull-left"><%= 1000 + @purchase.id %></span><br>
      <span class="pull-left"><%= @purchase.order_value %></span>
    </div>
  </div>
  <table class="table table-striped text-center">
    <thead>
      <tr class="thead-dark">
        <th scope="col">S.No</th>
        <th scope="col">Product Name</th>
        <th scope="col">Category</th>
        <th scope="col">Brand</th>
        <th scope="col">UOM</th>
        <th scope="col">Quantity</th>
        <th scope="col">Rate</th>
        <th scope="col">Amount</th>
      </tr>
    </thead>
    <tbody>
      <% serial_number = 1 %>
      <% @purchase_items.each do |item| %>
        <tr>
          <th scope="row"><%= serial_number %></th>
          <td><%= item.product.name %></td>
          <td><%= item.product.category.name %></td>
          <td><%= item.product.brand.name %></td>
          <td><%= item.product.uom.abbreviation %></td>
          <td><%= item.quantity %></td>
          <td><%= item.rate %></td>
          <td><%= item.amount %></td>
        </tr>
        <% serial_number += 1 %>
      <% end %>
    </tbody>
  </table>
</div>
